import { Component } from '@angular/core';

@Component({
  selector: 'app-callback',
  template: '<p>Loading...</p>', // Simple UI
})
export class CallbackComponent {
  // No additional logic needed
}
